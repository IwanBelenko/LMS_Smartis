# Smartis LMS

Планируемое название единой платформы: **HCM / LMS Smartis — система управления и обучения персонала**.

Бэклог расширения LMS до HCM / LMS: [docs/HR_BACKLOG.md](docs/HR_BACKLOG.md).

Корпоративная LMS для обучения сотрудников Smartis.

## Что уже работает

- вход по email и паролю;
- роли администратора, HR, автора, руководителя и сотрудника;
- матрица доступа: руководитель видит карточки только своего отдела, а оклады и премии открываются отдельным персональным разрешением;
- отделы и приглашения сотрудников;
- административный список пользователей;
- реестр сотрудников, штатная структура, кадровая история и HR-аналитика;
- безопасный массовый импорт сотрудников из CSV/XLSX с сопоставлением колонок, предварительной проверкой и защитой от дублей;
- временная синхронизация результатов iSpring из CSV/XLSX: сопоставление по корпоративной почте и названию курса, предпросмотр ошибок и журнал импортов;
- приглашения пользователей и восстановление пароля по одноразовым ссылкам через корпоративную почту;
- управление ролью, отделом и состоянием учётной записи с автоматической блокировкой при увольнении;
- защищённый журнал действий администратора с фильтрами по разделу, действию, сотруднику и периоду;
- центр задач и уведомлений с непрочитанными событиями, срочностью и быстрыми переходами;
- административные настройки профиля компании, корпоративных доменов, срока приглашений и каналов уведомлений;
- ежемесячные кадровые срезы из 1С с датой изменений, историей операций и защитой от повторного применения файла;
- вакансии и канбан-воронка кандидатов;
- планирование собеседований со сценарием вопросов, участниками, оценками и итоговым решением;
- офферы кандидатам с загрузкой PDF/DOCX, согласованием и фиксацией ответа;
- перевод кандидата в сотрудника без повторного ввода основных данных;
- конструктор курсов с текстовыми, видео-, ссылочными и файловыми уроками;
- визуальный редактор текста: заголовки, шрифты, размеры, цвета, списки, цитаты, ссылки и выравнивание;
- загрузка видео на платформу с хранением в отдельном Docker-томе;
- стандартные и пользовательские обложки курсов;
- импорт ZIP-пакетов SCORM 1.2 и экспорт курсов Smartis в SCORM 1.2;
- редактирование карточки импортированного SCORM-курса и замена пакета новой версией;
- преобразование лонгридов iSpring в редактируемую копию Smartis с главами, текстом и вопросами;
- предварительный просмотр обычных и SCORM-курсов прямо в LMS;
- рабочее прохождение назначенных курсов с сохранением прогресса по каждому уроку;
- последовательные тесты с проходным баллом, настраиваемым количеством попыток и историей результатов;
- семь форматов вопросов: один или несколько ответов, верно/неверно, сопоставление, порядок, короткий ответ и пропуск в тексте; к вопросу можно добавить изображение;
- ручное назначение курсов и траекторий сотрудникам, автоматический допуск к следующему курсу;
- черновики, версии, публикация и снятие курсов с публикации;
- отдельные права администратора, автора и сотрудника для работы с курсами;
- учебный дашборд и персональная «Сеть развития» на основе назначенных курсов;
- светлая и тёмная темы;
- запуск через Docker Compose.

## Запуск через Docker

~~~bash
cp .env.example .env
docker compose up --build
~~~

Откройте http://localhost. Для первого локального входа:

- email: admin@smartis.local
- пароль: SmartisDemo123!

Локальный шаблон включает `BOOTSTRAP_DEMO=true`. Он создаёт тестовых сотрудников
и материалы. В production этот режим необходимо выключить.

## Развёртывание на сервере

Подробная инструкция: [docs/DEPLOYMENT.md](docs/DEPLOYMENT.md).

Короткий сценарий:

~~~bash
cp .env.production.example .env
# Заполните домены, секреты и временный пароль первого администратора
docker compose config
docker compose up -d --build
docker compose ps
~~~

Caddy автоматически получает HTTPS-сертификаты, если оба домена направлены на
сервер и порты 80/443 открыты. SCORM размещается на отдельном поддомене — это
изолирует импортированный контент от интерфейса и авторизации системы.

После первого успешного входа очистите `INITIAL_ADMIN_EMAIL` и
`INITIAL_ADMIN_PASSWORD` в `.env`, затем выполните `docker compose up -d`.

## Состав проекта

- frontend — React + TypeScript;
- backend — Django REST API;
- infra — Caddy;
- compose.yaml — PostgreSQL, API, веб-интерфейс, Caddy и сервисы резервного копирования.
