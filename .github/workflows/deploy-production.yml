name: Deploy Smartis LMS to production

on:
  workflow_dispatch:
    inputs:
      tag:
        description: "Immutable release tag, for example v0.3.0"
        required: true
        type: string
      confirmation:
        description: "Type DEPLOY_PRODUCTION"
        required: true
        type: string

permissions:
  contents: read

concurrency:
  group: smartis-production
  cancel-in-progress: false

jobs:
  validate:
    name: Validate release tag
    runs-on: ubuntu-latest
    timeout-minutes: 5
    outputs:
      revision: ${{ steps.release.outputs.revision }}

    steps:
      - name: Require explicit confirmation
        env:
          CONFIRMATION: ${{ inputs.confirmation }}
        run: test "$CONFIRMATION" = "DEPLOY_PRODUCTION"

      - name: Check out release
        uses: actions/checkout@v4
        with:
          ref: ${{ inputs.tag }}
          fetch-depth: 0

      - name: Verify immutable tag from main
        id: release
        env:
          RELEASE_TAG: ${{ inputs.tag }}
        run: |
          case "$RELEASE_TAG" in
            v[0-9]*.[0-9]*.[0-9]*) ;;
            *) echo "Release tag must look like v0.3.0" >&2; exit 1 ;;
          esac
          test "$(git rev-parse "refs/tags/$RELEASE_TAG^{commit}")" = "$(git rev-parse HEAD)"
          git merge-base --is-ancestor HEAD origin/main
          echo "revision=$(git rev-parse HEAD)" >> "$GITHUB_OUTPUT"

  verify:
    name: Verify release
    needs: validate
    uses: ./.github/workflows/ci.yml
    with:
      ref: ${{ inputs.tag }}

  deploy:
    name: Backup and deploy
    needs:
      - validate
      - verify
    runs-on: ubuntu-latest
    timeout-minutes: 35
    environment:
      name: production
      url: https://lms.89.108.88.157.sslip.io/
    env:
      RELEASE_TAG: ${{ inputs.tag }}
      REVISION: ${{ needs.validate.outputs.revision }}
      SERVER_HOST: ${{ secrets.SERVER_HOST }}
      SERVER_PORT: ${{ secrets.SERVER_PORT }}
      SERVER_USER: ${{ secrets.SERVER_USER }}

    steps:
      - name: Check out verified release
        uses: actions/checkout@v4
        with:
          ref: ${{ inputs.tag }}

      - name: Build release archive
        run: |
          test "$(git rev-parse HEAD)" = "$REVISION"
          tar \
            --exclude=.git \
            --exclude=.env \
            --exclude=backend/.venv \
            --exclude=frontend/node_modules \
            --exclude=frontend/dist \
            --exclude=backups \
            -czf /tmp/smartis-release.tar.gz .

      - name: Configure SSH
        env:
          SSH_PRIVATE_KEY: ${{ secrets.SSH_PRIVATE_KEY }}
          SERVER_KNOWN_HOSTS: ${{ secrets.SERVER_KNOWN_HOSTS }}
        run: |
          test -n "$SERVER_HOST"
          test -n "$SERVER_PORT"
          test -n "$SERVER_USER"
          test -n "$SSH_PRIVATE_KEY"
          test -n "$SERVER_KNOWN_HOSTS"
          install -d -m 0700 ~/.ssh
          install -m 0600 /dev/null ~/.ssh/id_ed25519
          printf '%s\n' "$SSH_PRIVATE_KEY" > ~/.ssh/id_ed25519
          printf '%s\n' "$SERVER_KNOWN_HOSTS" > ~/.ssh/known_hosts

      - name: Upload immutable release
        run: |
          scp -P "$SERVER_PORT" /tmp/smartis-release.tar.gz \
            "$SERVER_USER@$SERVER_HOST:/tmp/smartis-$REVISION.tar.gz"
          scp -P "$SERVER_PORT" infra/deploy-production.sh \
            "$SERVER_USER@$SERVER_HOST:/tmp/smartis-deploy-$REVISION.sh"

      - name: Backup and activate release
        run: |
          ssh -p "$SERVER_PORT" "$SERVER_USER@$SERVER_HOST" \
            "REVISION='$REVISION' RELEASE_TAG='$RELEASE_TAG' \
             bash '/tmp/smartis-deploy-$REVISION.sh'; \
             status=\$?; rm -f '/tmp/smartis-deploy-$REVISION.sh'; exit \$status"

      - name: Verify activated revision
        run: |
          deployed_revision="$(ssh -p "$SERVER_PORT" "$SERVER_USER@$SERVER_HOST" \
            'cat /opt/smartis-releases/.deployed-revision')"
          active_release="$(ssh -p "$SERVER_PORT" "$SERVER_USER@$SERVER_HOST" \
            'readlink -f /opt/smartis-releases/current')"
          test "$deployed_revision" = "$REVISION $RELEASE_TAG"
          test "$active_release" = "/opt/smartis-releases/$REVISION"

      - name: Report deployed version
        run: echo "Deployed $RELEASE_TAG ($REVISION)"
